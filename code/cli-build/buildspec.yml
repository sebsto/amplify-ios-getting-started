version: 0.2

phases:
  install:
    commands:
      - SQSProducer $SQS_COMMAND_QUEUE_URL $SQS_RESPONSE_QUEUE_URL code/cli-build/codebuild_configuration.sh

  pre_build:
    commands:
      - echo "Pre build"
  
  build:
    commands:
      - echo "Build"

  post_build:
    commands:
      - echo "Post build"
